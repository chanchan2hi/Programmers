using System;

//앞으로는 깃에 올릴때 대문자 이름 사용해 !
public class Solution {
    public int solution(int num1, int num2) {
        if(num1==num2) return 1;
        else return -1;
      // return num1 == num2 ? 1 : -1; 이렇게 람다식 한 사람들이 많음.
    }
}
